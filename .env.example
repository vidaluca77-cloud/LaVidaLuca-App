# La Vida Luca App - Environment Configuration
# This is the root environment file for development setup

# Quick Start:
# 1. Copy this file to .env
# 2. Update the values below for your local environment
# 3. Copy apps/backend/.env.example to apps/backend/.env and configure backend
# 4. Copy apps/web/.env.local.example to apps/web/.env.local and configure frontend

# Global Settings
APP_NAME=La Vida Luca
APP_VERSION=1.0.0
ENVIRONMENT=development

# Database Configuration (shared by backend)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=lavidaluca_dev

# Production Database (PostgreSQL on Render)
DATABASE_URL=postgresql://username:password@hostname:port/database

# API Configuration
BACKEND_PORT=8000
FRONTEND_PORT=3000
IA_SERVICE_PORT=8001

# Security
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
SECRET_KEY=your-backend-secret-key-change-in-production

# External Services
OPENAI_API_KEY=your-openai-api-key-here

# Monitoring - Sentry Configuration
SENTRY_DSN=your-sentry-dsn-here
SENTRY_ORG=your-sentry-organization
SENTRY_PROJECT_FRONTEND=lavidaluca-frontend
SENTRY_PROJECT_BACKEND=lavidaluca-backend
SENTRY_AUTH_TOKEN=your-sentry-auth-token
SENTRY_ENVIRONMENT=development

# Development URLs
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_IA_API_URL=http://localhost:8001

# Production URLs (update these for deployment)
PRODUCTION_API_URL=https://lavidaluca-backend.onrender.com/api/v1
PRODUCTION_BACKEND_URL=https://lavidaluca-backend.onrender.com
PRODUCTION_FRONTEND_URL=https://lavidaluca.vercel.app

# Deployment Secrets (for GitHub Actions)
# These should be configured in GitHub repository secrets:
# - VERCEL_TOKEN
# - VERCEL_ORG_ID  
# - VERCEL_PROJECT_ID
# - RENDER_DEPLOY_HOOK_BACKEND
# - RENDER_DEPLOY_HOOK_IA